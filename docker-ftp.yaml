FROM ubuntu:latest
RUN apt-gep update && \ apt-get install -y vsftpd && \ apt-get clean
COPY vsftpd.conf /etc/vsftpd.conf
RUN useradd -m usuario_ftp && \ echo "usuario_ftp:pass_ftp" | chpasswd
RUN chmod +x /home/maria/ftp.sh
EXPOSE 21 
CMD ["/home/maria/ftp.sh"]